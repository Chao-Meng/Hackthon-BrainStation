function createDiv(className) {
  let divName = document.createElement("div");
  divName.classList.add(className);
  return divName;
}
function createImg(className) {
  let imgName = document.createElement("img");
  imgName.classList.add(className);
  return imgName;
}
